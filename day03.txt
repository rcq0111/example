git branch
* main
git branch temp
git switch temp
main -> temp
git checkout main 
temp -> main
git merge temp
main에 temp 병합

git branch -d temp
temp삭제

git log --oneline
git log --oneline --graph

git remote -v 현재 원격 로칼 주소만 알 수 있다.


fork 
clone
branch
switch (checkout)
소스코드 수정(add /commit/push(push -u origin ____))
pull request (memo)
branch -d
원본 리포지토리 clone